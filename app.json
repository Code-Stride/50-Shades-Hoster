{
  "name": "50 Shades Hoster",
  "description": "Powerful Telegram bot hosting system supporting automatic dependency installations, active watchdog monitoring, and API 9.4 colored buttons.",
  "repository": "https://github.com/Code-Stride/50-Shades-Hoster",
  "keywords": ["telegram", "bot", "hosting", "python", "node", "watchdog"],
  "env": {
    "BOT_TOKEN": {
      "description": "Your Telegram Bot Token from @BotFather",
      "required": true
    },
    "OWNER_ID": {
      "description": "Your Telegram User ID (Get from @userinfobot)",
      "required": true
    },
    "ADMIN_ID": {
      "description": "Optional: Admin Telegram User ID (Defaults to OWNER_ID)",
      "required": false
    },
    "YOUR_USERNAME": {
      "description": "Optional: Your Telegram Username (without @)",
      "required": false
    },
    "UPDATE_CHANNEL": {
      "description": "Optional: Channel username for mandatory subscriptions (without @)",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  }
}
